const Query = require('./query')
const Mutation = require('./mutation')
// const {GraphQLDateTime} = require('graphql-iso-date')

//change this to resolvers
module.exports = {
	Query,
	Mutation,
	// DateTime: GraphQLDateTime
}

